"""Database boilerplate, all in one place: engine, sessionmaker, and the
get_db() dependency that routes/services receive via Depends().

Schema lives in app/core/models.py (SQLAlchemy models) + alembic/ migrations.
"""

# TODO(dev): Step 3 — hand-write engine, SessionLocal, get_db() here.
